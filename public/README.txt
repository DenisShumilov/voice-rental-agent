Put the three product photographs here, with exactly these names:

  camera-a.jpg
  tripod-b.jpg
  microphone-c.jpg

They are referenced from src/config/catalog.ts. A missing file degrades to a
typographic tile rather than a broken image, so the page never looks broken.
